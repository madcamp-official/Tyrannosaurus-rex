# Backend 변경 이력

## 2026-07-30 - 조준 이벤트 빈도 제한

- 구현 목적: 여러 플레이어가 동시에 조준할 때 호스트 사격 화면에 누적되는 이벤트·렌더링 부하를 줄였다.
- 주요 변경 사항: 서버가 허용하고 모바일이 전송하는 조준 좌표 갱신 상한을 30Hz에서 15Hz로 낮췄다.
- API/Socket.IO/Shared 계약 변경: 이벤트 구조 변경 없이 `AIM_UPDATE_MAX_HZ` 공유 상수 값만 변경했다.
- 환경 변수 변경: 없음.
- 검증 결과: Backend 테스트 62개 및 전체 프로덕션 빌드 통과.

## 2026-07-29 - 운석 낙하 밀도 증가

- 구현 목적: 공룡 달리기 단계에서 운석이 더 자주 떨어지고 여러 개가 동시에 낙하하도록 난이도와 화면 밀도를 높였다.
- 주요 변경 사항: 1분간 생성되는 하늘 오브젝트 수를 60개에서 110개로 늘리고, 최소 생성 간격을 650ms에서 250ms로 줄였다. 낙하 시간 800ms 동안 최소 3개가 동시에 존재할 수 있는지 검증하는 회귀 테스트를 추가했다.
- API/Socket.IO/Shared 계약 변경: 이벤트 구조 변경 없이 `SKY_OBJECT_COUNT`와 `SKY_OBJECT_MIN_GAP_MS` 공유 상수 값만 변경했다.
- 환경 변수 변경: 없음.
- 검증 결과: 90개 설정에서는 특정 시드의 최대 동시 낙하 수가 2개라 회귀 테스트가 실패해 110개로 조정했다. 조정 후 Backend 테스트 62개 및 전체 프로덕션 빌드 통과.
