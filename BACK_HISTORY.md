# Backend 변경 이력

## 2026-07-30 - 조준 이벤트 빈도 제한

- 구현 목적: 여러 플레이어가 동시에 조준할 때 호스트 사격 화면에 누적되는 이벤트·렌더링 부하를 줄였다.
- 주요 변경 사항: 서버가 허용하고 모바일이 전송하는 조준 좌표 갱신 상한을 30Hz에서 15Hz로 낮췄다.
- API/Socket.IO/Shared 계약 변경: 이벤트 구조 변경 없이 `AIM_UPDATE_MAX_HZ` 공유 상수 값만 변경했다.
- 환경 변수 변경: 없음.
- 검증 결과: Backend 테스트 62개 및 전체 프로덕션 빌드 통과.

## 2026-07-29 - 운석 낙하 밀도 증가

- 구현 목적: 공룡 달리기 단계에서 운석이 더 자주 떨어지고 여러 개가 동시에 낙하하도록 난이도와 화면 밀도를 높였다.
- 주요 변경 사항: 1분간 생성되는 하늘 오브젝트 수를 60개에서 110개로 늘리고, 최소 생성 간격을 650ms에서 250ms로 줄였다. 낙하 시간 800ms 동안 최소 3개가 동시에 존재할 수 있는지 검증하는 회귀 테스트를 추가했다.
- API/Socket.IO/Shared 계약 변경: 이벤트 구조 변경 없이 `SKY_OBJECT_COUNT`와 `SKY_OBJECT_MIN_GAP_MS` 공유 상수 값만 변경했다.
- 환경 변수 변경: 없음.
- 검증 결과: 90개 설정에서는 특정 시드의 최대 동시 낙하 수가 2개라 회귀 테스트가 실패해 110개로 조정했다. 조정 후 Backend 테스트 62개 및 전체 프로덕션 빌드 통과.
## 2026-07-29 - 비공개 토큰 기반 참가자 재접속

- 구현 목적: 모바일 사용자가 연결이 끊긴 뒤 같은 방 주소로 돌아왔을 때 기존 참가자만 진행 중인 게임에 복귀할 수 있게 했다.
- 주요 변경 사항: 서버가 참가자별 UUID 재접속 토큰을 메모리에 보관하고, 닉네임과 토큰이 모두 일치할 때 기존 player ID·팀·색상·게임 상태를 유지한 채 새 소켓에 다시 연결한다. 잘못된 토큰은 시작된 방에 새 참가자로 들어갈 수 없다.
- API/Socket.IO/Shared 계약 변경: `room:join` 요청의 선택적 `reconnectToken`, 응답의 `reconnectToken`과 `reconnected` 필드를 추가했다. 정상 복귀 시 신규 입장 이벤트 대신 연결 상태 변경 이벤트를 전파한다.
- 환경 변수 변경: 없음.
- 검증 결과: 올바른 토큰 복구와 잘못된 토큰 거부 테스트를 추가했으며 `npm run typecheck`, `npm test`(74개), `npm run build`, `git diff --check` 통과.

## 2026-07-30 - 명중 기반 공유 약점 이동

- 구현 목적: 사격 단계에서 현재 약점을 정확히 맞히는 플레이어만 보상받고, 명중할 때마다 새로운 사격 지점을 추적하게 해 경쟁성과 조준 재미를 높였다.
- 주요 변경 사항: 방 단위 공유 활성 코어와 명중 순번을 서버 상태로 추가했다. 현재 약점 명중만 개인 명중·기여 점수와 팀 에너지로 인정하며 몸체 사격은 무득점 처리한다. 유효 명중 직후 현재 부위를 제외한 다음 약점을 결정적으로 선택해 양 팀 상태에 적용한다.
- API/Socket.IO/Shared 계약 변경: 기존 `energy:coreChanged` 이벤트를 명중 직후 양 팀에 전파한다. 이벤트 구조는 변경하지 않았다.
- 환경 변수 변경: 없음.
- 검증 결과: 약점 즉시 변경, 양 팀 동기화, 몸체 무득점 회귀 테스트를 추가했다.
