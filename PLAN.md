# 🦖 Tyrannosaurus Rex — 개발 계획

> 몰입캠프 26s-w4-c3-01
>
> 웹 기반 무가입 크로스플랫폼 파티 게임
>
> 데스크탑은 공유 게임 화면, 각자의 스마트폰은 컨트롤러로 사용한다.

---

## 1. 프로젝트 목표

같은 HTTPS 주소에서 데스크탑이 방을 만들고 최대 5대의 스마트폰이 접속하여 아래 게임 흐름을 안정적으로 완주하는 것을 목표로 한다.

```text
로비
  → 1페이즈: 땅 파기
  → 티라노 발견 연출
  → 2페이즈: 티라노 사격
  → 결과
  → 재경기
```

### 핵심 원칙

- 별도 회원가입 없이 4자리 방 코드와 QR로 입장한다.
- 모든 게임 상태와 판정은 서버가 관리한다.
- 모바일은 센서와 터치 입력만 집계하여 서버로 보낸다.
- iOS 센서 제약에 대응하도록 전체 서비스를 HTTPS로 제공한다.
- 자이로를 사용할 수 없는 환경에서도 터치패드 조준으로 게임을 완주할 수 있어야 한다.
- 7일 안에 시연 가능한 핵심 게임 루프를 먼저 완성하고 부가 콘텐츠는 후순위로 둔다.

---

## 2. 확정 게임 규칙

### 2.1 공통 규칙

| 항목 | 규칙 |
|---|---|
| 참가 인원 | 개인전·팀전 모두 2~5명 |
| 닉네임 | 공백 제거 후 1~8자, 같은 방에서 중복 불가 |
| 방 코드 | 숫자 4자리 |
| 팀 배정 | 더 적은 팀에 자동 배정, 동률이면 A/B 교대 |
| 신규 입장 | 게임 시작 후 입장 불가 |
| 재경기 | 기존 참가자·색상·팀을 유지하고 로비로 복귀 |

### 2.2 개인전

- 각 플레이어가 독립적으로 땅을 판다.
- 현재 참가자의 과반인 `ceil(참가자 수 / 2)`명이 목표 깊이에 도달하면 사격 페이즈로 전환한다.
- 누적 데미지가 가장 높은 플레이어가 승리한다.
- 동점이면 명중률, 그다음 최초 유효 명중 시각 순으로 순위를 정한다.

### 2.3 팀전

- 서버가 플레이어를 A/B팀으로 균형 배정한다.
- 팀원의 파기 기여량을 합산하여 팀 깊이를 계산한다.
- 어느 한 팀이 목표 깊이에 도달하면 사격 페이즈로 전환한다.
- 팀 합산 데미지가 높은 팀이 승리한다.
- 동점이면 팀 명중률, 그다음 마지막 유효 명중이 빠른 팀을 우선한다.

### 2.4 MVP 우선순위

반드시 구현할 기능:

- 방 생성, QR 입장, 모드 선택, 팀 배정
- 탭 및 흔들기 파기
- 개인전·팀전 땅 파기 화면과 종료 조건
- 터치패드 조준
- 자이로 권한 요청과 캘리브레이션
- 사격, 서버 히트 판정, 티라노 HP
- 결과, 통계, 재경기

핵심 루프 안정화 후 추가할 기능:

- 돌덩이와 화석
- 막타 보너스
- 고급 애니메이션과 사운드
- 추가 티라노 패턴

MVP 범위에서 제외할 기능:

- 진행 중 재접속 및 자리 복구
- 6명 이상 대기열
- 관전 모드
- 방해 아이템
- 영구 저장 및 전적 시스템

---

## 3. 시스템 아키텍처

```text
[데스크탑 브라우저]
        │
        │ Socket.IO
        ▼
[Node.js 권위 서버]
        ▲
        │ Socket.IO
        │
[모바일 브라우저 × 2~5]
```

| 패키지 | 기술 | 역할 |
|---|---|---|
| `shared/` | TypeScript | 도메인 타입, Socket.IO 이벤트 타입, 게임 상수 |
| `server/` | Node.js, Socket.IO, TypeScript | 방 관리, 상태 머신, 깊이 계산, 사격 판정 |
| `client/` | React, Vite, PixiJS, TypeScript | 데스크탑 공유 화면과 모바일 컨트롤러 |

### 3.1 프로젝트 구조

```text
Tyrannosaurus-rex/
├─ package.json
├─ PLAN.md
├─ shared/
│  └─ src/
│     └─ index.ts
├─ server/
│  ├─ src/
│  │  └─ index.ts
│  └─ test/
└─ client/
   └─ src/
      ├─ desktop/
      ├─ mobile/
      ├─ socket.ts
      └─ main.tsx
```

### 3.2 라우팅 및 HTTPS

- `/`: 데스크탑 공유 화면
- `/join/:code`: 모바일 컨트롤러
- Vite가 `/socket.io` 요청을 `localhost:3001`로 프록시한다.
- 서버는 `0.0.0.0:3001`, Vite는 `0.0.0.0:5173`에 바인딩한다.
- Cloudflare Tunnel 하나로 Vite와 Socket.IO를 동일 HTTPS origin에 노출한다.
- QR은 현재 브라우저의 HTTPS origin을 우선 사용한다.
- 터널이 없을 때만 LAN IP 주소를 개발용 폴백으로 사용한다.

### 3.3 개발 명령어

```bash
npm install
npm run dev
npm run dev:tunnel
npm run typecheck
npm test
npm run build
npm run simulate -- --players 5
```

---

## 4. 공유 데이터 모델

### 4.1 게임 상태

```ts
type GamePhase =
  | "LOBBY"
  | "DIGGING"
  | "DISCOVERY"
  | "SHOOTING"
  | "RESULT";

type GameMode = "SOLO" | "TEAM";
type InputMode = "GYRO" | "TOUCHPAD";
```

허용하는 상태 전환:

```text
LOBBY → DIGGING → DISCOVERY → SHOOTING → RESULT
  ▲                                          │
  └──────────────── REMATCH ─────────────────┘
```

클라이언트가 임의로 상태를 변경할 수 없으며 서버만 다음 페이즈를 결정한다.

### 4.2 주요 타입

#### `Player`

- 플레이어 ID
- 닉네임
- 색상
- 소속 팀
- 연결 상태
- 현재 깊이
- 누적 데미지
- 발사 수와 명중 수
- 약점 명중 수
- 최신 크로스헤어 좌표

#### `RoomState`

- 방 코드와 데스크탑 호스트 ID
- 게임 모드와 현재 페이즈
- 참가자 목록
- 개인 및 팀 깊이
- 티라노 상태
- 페이즈 시작·종료 시각
- 최종 결과

#### `Crosshair`

- `x`, `y`는 해상도와 무관한 `0~1` 정규화 좌표를 사용한다.
- 서버는 범위를 벗어난 값, `NaN`, 무한대를 거부한다.

#### `TyrannoState`

- 현재 HP와 최대 HP
- 정규화된 위치
- 이동 방향
- 몸통·눈·꼬리 히트박스

---

## 5. Socket.IO 인터페이스

### 5.1 클라이언트 → 서버

| 이벤트 | 용도 |
|---|---|
| `room:create` | 데스크탑 방 생성 |
| `room:join` | 방 코드와 닉네임으로 모바일 입장 |
| `room:setMode` | 로비에서 개인전·팀전 변경 |
| `game:start` | 참가자 2명 이상일 때 게임 시작 |
| `dig:input` | 100ms 동안 집계한 파기 횟수 전송 |
| `aim:update` | 정규화된 크로스헤어 좌표 전송 |
| `shot:fire` | 고유 요청 ID가 포함된 발사 요청 |
| `sensor:status` | 권한 및 센서 지원 상태 전달 |
| `game:rematch` | 결과 화면에서 로비로 복귀 |

### 5.2 서버 → 클라이언트

| 이벤트 | 용도 |
|---|---|
| `room:created` | 방 코드와 참가 URL 전달 |
| `room:joined` | 플레이어 ID, 색상, 팀 전달 |
| `room:state` | 권위 상태 스냅샷 전달 |
| `dig:update` | 깊이와 순위 변경 전달 |
| `game:phaseChanged` | 새 페이즈와 전환 시각 전달 |
| `aim:update` | 검증된 크로스헤어를 데스크탑에 전달 |
| `shot:result` | 명중 부위, 데미지, 남은 HP 전달 |
| `game:result` | 최종 순위와 통계 전달 |
| `room:error` | 안정적인 오류 코드와 메시지 전달 |
| `room:closed` | 데스크탑 종료로 인한 방 폐쇄 안내 |

요청 결과가 필요한 이벤트는 다음 acknowledgement 형식을 사용한다.

```ts
type Ack<T> =
  | { ok: true; data: T }
  | { ok: false; error: { code: string; message: string } };
```

`room:state`는 입장, 모드 변경, 페이즈 변경 등 저빈도 상태 동기화에 사용한다. 30Hz 조준 좌표는 별도 `aim:update` 이벤트로 전송하여 전체 상태 브로드캐스트 비용을 줄인다.

---

## 6. 연결 및 로비

### 6.1 방 생성

1. 데스크탑이 `/`에 접속한다.
2. 서버가 중복되지 않는 숫자 4자리 코드를 생성한다.
3. 데스크탑에 방 코드, HTTPS 참가 URL, QR을 표시한다.
4. 데스크탑 소켓 ID를 해당 방의 호스트로 기록한다.

### 6.2 모바일 입장

1. QR을 통해 `/join/:code`로 접속한다.
2. 1~8자 닉네임을 입력한다.
3. 서버가 방 존재 여부, 페이즈, 정원, 닉네임 중복을 검사한다.
4. 서버가 색상과 팀을 자동 배정한다.
5. 모든 클라이언트에 갱신된 로비 상태를 전송한다.

### 6.3 연결 해제

- 데스크탑이 연결 해제되면 방을 즉시 폐쇄한다.
- 모바일이 게임 도중 연결 해제되면 해당 플레이어 자리는 유지하되 입력을 중지한다.
- 연결 해제 플레이어는 현재 판의 전환 조건과 순위 계산에는 포함한다.
- 모든 활성 플레이어가 연결 해제되면 현재 게임을 종료한다.
- MVP에서 새로고침은 새 입장으로 취급하며 진행 중인 방에는 재입장할 수 없다.

---

## 7. 1페이즈 — 땅 파기

### 7.1 모바일 입력

#### 흔들기

- 센서 활성화 버튼을 사용자가 직접 눌렀을 때 권한을 요청한다.
- iOS에서는 해당 탭 핸들러 안에서 `DeviceMotionEvent.requestPermission()`을 호출한다.
- 중력을 제외한 가속도 벡터의 크기가 임계값을 넘으면 1회로 집계한다.
- 200ms 쿨다운으로 한 번의 흔들림이 여러 번 집계되는 것을 방지한다.

#### 탭

- 센서 지원 여부와 관계없이 큰 파기 버튼을 항상 제공한다.
- 흔들기와 탭 횟수는 같은 파기 입력으로 합산한다.

#### 전송

- 모바일은 100ms마다 해당 구간의 파기 횟수만 서버에 보낸다.
- 페이지가 백그라운드로 이동하면 센서 리스너와 미전송 입력을 정리한다.
- 서버는 플레이어당 초당 최대 12회의 파기만 인정한다.

### 7.2 서버 처리

- 방, 플레이어, 현재 페이즈를 검증한다.
- 음수, 비정상 값, 허용량을 초과한 입력을 거부한다.
- 개인전은 플레이어별 깊이를 증가시킨다.
- 팀전은 팀원 기여량을 팀 깊이에 합산한다.
- 개인전 목표 깊이는 플레이어당 1000이다.
- 팀전 목표 깊이는 `1000 × 해당 팀 인원`이다.
- 동시 도달 시 서버 수신 시각과 누적 깊이를 기준으로 순위를 정한다.

### 7.3 종료 조건

- 개인전: `ceil(현재 참가자 수 / 2)`명이 깊이 1000에 도달
- 팀전: 어느 한 팀이 목표 깊이에 도달
- 조건이 충족되면 서버가 파기 입력을 잠그고 `DISCOVERY`로 전환한다.
- 3초간 티라노 발견 연출 후 `SHOOTING`으로 전환한다.

### 7.4 데스크탑 화면

- 개인전은 참가 인원만큼 균등한 세로 레인을 만든다.
- 팀전은 A/B 두 영역을 고정한다.
- 팀 영역 안에는 각 팀원의 개별 기여 바를 표시한다.
- 닉네임, 색상, 깊이, 순위, 연결 상태를 표시한다.
- PixiJS는 지층, 캐릭터, 흙 파편 애니메이션을 담당한다.
- React는 HUD, 순위, 페이즈 안내를 담당한다.

### 7.5 2페이즈 어드밴티지

- 개인전에서 목표 깊이에 도달한 플레이어는 사격 시작 시 보너스 데미지 50점을 얻는다.
- 팀전에서 먼저 목표 깊이에 도달한 팀은 팀원 모두 첫 명중에 5점 추가 데미지를 얻는다.
- 플레이어별 선발 사격은 동기화와 공정성 문제 때문에 MVP에서 제외한다.

---

## 8. 2페이즈 — 티라노 사격

### 8.1 기본 수치

| 항목 | 초기값 |
|---|---:|
| 제한 시간 | 60초 |
| 티라노 최대 HP | `참가자 수 × 500` |
| 몸통 데미지 | 10 |
| 꼬리 데미지 | 15 |
| 눈 데미지 | 20 |
| 발사 쿨다운 | 300ms |
| 조준 좌표 전송 | 최대 30Hz |
| 티라노 위치 갱신 | 서버 10Hz |

게임 밸런스 수치는 `shared` 상수로 관리하여 코드 수정 범위를 최소화한다.

### 8.2 터치패드 조준

- 모든 기기에서 항상 사용할 수 있는 기본 폴백이다.
- 모바일 화면의 드래그 변화량을 현재 크로스헤어 좌표에 더한다.
- 결과 좌표를 `0~1`로 제한하여 서버에 전송한다.
- 자이로 권한이 거절되거나 조준 품질이 낮으면 즉시 터치패드로 전환할 수 있다.

### 8.3 자이로 조준

1. 사용자가 센서 권한을 허용한다.
2. 폰을 데스크탑 화면 중앙에 겨눈다.
3. `조준 시작` 버튼을 눌러 현재 `alpha`, `beta`, `gamma`를 영점으로 저장한다.
4. 이후 상대 회전량을 크로스헤어 이동량으로 변환한다.
5. 저역 통과 필터와 감도 계수를 적용한다.
6. 화면 방향이 바뀌면 재캘리브레이션을 요구한다.

자이로 지원, 권한 허용, 캘리브레이션 성공을 모두 만족할 때만 자이로 모드를 활성화한다.

### 8.4 발사와 히트 판정

- 모바일 화면 탭으로 발사한다.
- 발사 요청에는 중복 처리를 방지할 고유 요청 ID를 포함한다.
- 서버는 발사 시점의 서버 권위 크로스헤어 좌표를 사용한다.
- 히트박스가 겹치면 `눈 → 꼬리 → 몸통` 순서로 판정한다.
- 서버가 부위별 데미지를 계산하고 HP와 점수를 갱신한다.
- 클라이언트가 보낸 데미지나 명중 결과는 신뢰하지 않는다.
- HP가 0이 되거나 제한 시간이 끝나면 추가 발사를 거부한다.

### 8.5 데스크탑 렌더링

- 플레이어 색상의 크로스헤어를 최대 5개 동시에 표시한다.
- 서버가 보낸 티라노 위치를 클라이언트에서 보간한다.
- 명중 부위별 이펙트와 데미지 숫자를 표시한다.
- HP, 남은 시간, 개인 또는 팀별 누적 데미지를 표시한다.
- 마지막 유효 공격자는 기록하지만 막타 보너스는 기본 비활성화한다.

---

## 9. 결과 및 재경기

결과 화면에 다음 항목을 표시한다.

- 개인 또는 팀 최종 순위
- 총 데미지
- 발사 수
- 명중 수
- 명중률
- 약점 명중 수
- 마지막 유효 공격자

데스크탑만 재경기를 요청할 수 있다. 재경기 시 참가자, 색상, 팀은 유지하고 다음 항목만 초기화한다.

- 깊이와 파기 순위
- 데미지와 사격 통계
- 크로스헤어
- 티라노 HP와 위치
- 센서 캘리브레이션 상태
- 페이즈 타이머

---

## 10. 7일 구현 일정

### Day 1 — 프로젝트 기반과 로비

#### 구현

- npm workspaces와 TypeScript 설정
- React/Vite 클라이언트와 Socket.IO 서버 생성
- 공유 타입과 이벤트 맵 작성
- 방 생성, 입장, 모드 변경, 팀 배정, 게임 시작 구현
- QR과 Cloudflare Tunnel 구성
- 데스크탑·모바일 기본 반응형 화면 구현
- 가짜 모바일 클라이언트 스크립트 작성

#### 완료 기준

- HTTPS QR을 스캔한 실기기 2대가 방에 입장한다.
- 명단, 팀, 모드, 페이즈가 모든 화면에서 동일하다.
- 2~5명의 시뮬레이션 클라이언트가 정상 입장한다.

### Day 2 — 모바일 입력과 센서 검증

#### 구현

- 탭 파기
- 흔들기 감지와 200ms 쿨다운
- iOS 센서 권한 요청
- 권한 거절·미지원 UI
- 100ms 입력 배치
- 서버 입력 속도 제한
- 실기기 입력 진단 패널

#### 완료 기준

- iOS와 Android에서 탭 파기가 동작한다.
- 지원 기기에서 흔들기 입력이 서버에 반영된다.
- 연속 20회 흔들기에서 중복 폭주가 발생하지 않는다.
- 권한 거절 후에도 탭으로 게임을 계속할 수 있다.

### Day 3 — 땅 파기 페이즈 완성

#### 구현

- 개인 및 팀 깊이 계산
- 동적 과반과 팀 도달 종료 조건
- PixiJS 개인 레인 및 팀 영역
- 깊이 HUD와 실시간 순위
- 티라노 발견 전환 연출
- 동시 도달과 연결 해제 처리

#### 완료 기준

- 2명과 5명, 개인전과 팀전의 네 조합이 모두 `DISCOVERY`까지 자동 진행된다.
- 비정상 파기 입력이 깊이에 반영되지 않는다.
- 연결 해제 플레이어가 있어도 서버 상태가 일관되게 유지된다.

### Day 4 — 조준 프로토타입과 폴백

#### 구현

- 터치패드 조준
- 자이로 권한과 캘리브레이션
- 상대 회전량 변환과 필터
- 조준 좌표 30Hz 제한
- 데스크탑 크로스헤어 표시
- 입력 모드 전환 UI

#### 완료 기준

- 모든 실기기에서 터치패드 조준이 가능하다.
- 자이로는 중앙 영점 후 화면 주요 영역을 안정적으로 가리킨다.
- 자이로 실패 시 게임 중 터치패드로 즉시 전환할 수 있다.

### Day 5 — 사격 페이즈 완성

#### 구현

- 티라노 이동
- 몸통·눈·꼬리 히트박스
- 발사 쿨다운과 중복 요청 차단
- 부위별 데미지와 HP
- 크로스헤어 최대 5개 표시
- 명중 이펙트
- HP 및 시간 종료 처리

#### 완료 기준

- 5개 시뮬레이션 클라이언트가 동시에 조준하고 발사할 수 있다.
- 중복 데미지와 종료 후 데미지가 발생하지 않는다.
- HP 소진과 시간 종료가 각각 결과 화면으로 정확히 이어진다.

### Day 6 — 전체 게임 루프와 콘텐츠

#### 구현

- 최종 순위와 통계
- 재경기
- 1페이즈 어드밴티지 연결
- 사운드와 전환 연출
- 모바일 진동과 안내 문구
- 핵심 루프가 안정적이면 돌덩이와 화석 추가

#### 완료 기준

- 서버를 재시작하지 않고 두 판 연속 완주한다.
- 재경기 시 점수와 게임 상태가 정확히 초기화된다.
- 개인전과 팀전 결과가 각각 올바르게 계산된다.

### Day 7 — 통합 테스트와 시연 동결

#### 검증

- 실기기 최대 5대 테스트
- 개인전·팀전 반복 플레이
- Cloudflare Tunnel, 캠퍼스 Wi-Fi, 핫스팟 테스트
- 센서 권한 거절, 화면 회전, 백그라운드 전환 테스트
- 데스크탑 및 모바일 연결 해제 테스트
- 성능 프로파일링과 밸런스 조정
- 시연 절차와 장애 대응 체크리스트 작성

#### 완료 기준

- 실기기 5대로 세 판 연속 완주한다.
- 자이로 실패 시 30초 안에 터치패드로 전환하여 계속 플레이한다.
- 기능을 동결하고 이후에는 치명적 버그만 수정한다.

---

## 11. 테스트 계획

### 11.1 단위 테스트

- 4자리 방 코드 생성과 충돌 시 재생성
- 닉네임 공백, 길이, 중복 검증
- 2~5명 개인전의 동적 과반 계산
- 홀수 인원 팀의 3:2 균형 배정
- 모드 변경 시 팀 재배정
- 파기 입력 속도 제한
- 정규화 좌표 검증
- 눈·꼬리·몸통 히트박스 경계
- 개인 및 팀 순위 동점 처리

### 11.2 통합 테스트

- 존재하지 않는 방 입장 거절
- 정원이 찬 방 입장 거절
- 진행 중인 방 입장 거절
- 동일 발사 ID 중복 전송 차단
- 발사 쿨다운 검증
- HP 소진과 타이머 종료가 동시에 발생할 때 결과 1회 확정
- 데스크탑 종료 시 방 폐쇄
- 모바일 종료 시 자리 유지
- 재경기 상태 초기화

### 11.3 실기기 테스트

- iOS 센서 권한 허용과 거절
- Android 센서 지원 여부
- 화면 방향 변경 후 재캘리브레이션
- 앱 백그라운드 전환
- 자이로에서 터치패드로 전환
- 5명 기준 30Hz 조준 전송 부하
- 네트워크 지연과 순간 연결 단절

### 11.4 전체 흐름 테스트

```text
방 생성
  → 모바일 5명 입장
  → 모드 선택
  → 땅 파기
  → 티라노 발견
  → 조준 및 사격
  → 결과
  → 재경기
```

개인전과 팀전에서 위 흐름을 각각 최소 3회 연속 완주한다.

---

## 12. 리스크 관리

| 리스크 | 대응 |
|---|---|
| iOS 센서 권한 및 HTTPS | Day 2 이전에 Cloudflare Tunnel과 실기기 권한 요청을 검증 |
| 자이로 조준 품질 | Day 4에 집중 검증하고 터치패드를 항상 제공 |
| Wi-Fi 지연 및 불안정 | 서버 권위 구조, 좌표 스로틀, 핫스팟 백업 |
| 실기기 5대 테스트 인력 부족 | Socket.IO 시뮬레이션 클라이언트 사용 |
| 과도한 기능 범위 | 핵심 루프 완료 전 아이템과 고급 연출 구현 금지 |
| 부정 입력 또는 치팅 | 서버 값 검증, 속도 제한, 서버 히트 판정 |
| 서버 재시작 | MVP에서는 방 소멸을 허용하고 시연 전 프로세스 상태 확인 |

---

## 13. 구현 가정

- 현재 저장소에는 문서 외 구현물이 없으므로 모든 코드를 신규 구축한다.
- 개인전과 팀전 모두 최대 5명으로 제한한다.
- 개인전 전환 기준은 고정 3명이 아니라 현재 참가자의 동적 과반이다.
- Cloudflare Tunnel을 기본 HTTPS 진입점으로 사용한다.
- 게임 상태는 서버 메모리에만 저장하며 서버 재시작 시 방이 사라지는 것을 허용한다.
- 자이로는 선택 기능이며 터치패드는 항상 사용할 수 있다.
- 돌덩이와 화석은 Day 5까지 핵심 루프가 안정화된 경우에만 추가한다.
- 전용 아트와 사운드가 준비되지 않으면 도형 및 라이선스가 확인된 임시 에셋을 사용한다.
