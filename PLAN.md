# 🦖 내 티라노사우루스 살려내!!! — 개발 계획

> 몰입캠프 26s-w4-c3-01 · 웹 기반 무가입 크로스플랫폼 파티 게임
> 데스크탑 = 모두가 보는 공유 화면 / 각자의 폰 = 컨트롤러

---

## 1. 게임 개요

- **인원**: **2팀전**, 팀당 최대 10명 (총 최대 20명)
- **스토리**: 죽은 티라노사우루스를 팀원들이 힘을 합쳐 되살린다. 넌 내 소중한 티라노야!!❤️
- **가입 없음**: 데스크탑에서 방 생성 → 4자리 코드 + QR 표시 → 폰으로 QR 스캔 → 닉네임만 입력하고 입장
- **1페이즈 — 뼈 캐기**: 폰을 **흔들어서** 땅속에 묻힌 티라노의 뼈를 캔다. 데스크탑에는 2분할 화면으로 팀별 진행이 보인다. 팀이 필요한 뼈를 모두 모으면 다음 단계 준비 완료
- **2페이즈 — 뼈 맞추기(퍼즐)**: 모은 뼈로 스켈레톤 티라노를 조립하는 협동 퍼즐
- **3페이즈 — 되살리기(에너지파 사격)**: 완성된 스켈레톤 티라노(위치 계속 이동)에게 폰을 총처럼 들고 자이로로 조준, 탭으로 레이저(에너지파)를 쏴 명중시킨다. 팀 에너지 총량이 목표치에 먼저 도달하면 완전 부활 성공
- **실패 조건**: 제한시간 내 에너지 목표치를 채우지 못하면 **좀비 티라노**로 변해버림 ☠️ (배드엔딩)
- **승리**: 목표치를 먼저 달성하는 팀이 이김

### 게임 플로우

```
로비 (2팀 자동 배정)
  → 1페이즈: 뼈 캐기 (2분할 화면, 흔들기)
  → 전환 연출 ("뼈를 모두 모았다! 조립 시작")
  → 2페이즈: 뼈 맞추기 퍼즐 (협동)
  → 전환 연출 ("이제 생명을 불어넣자!")
  → 3페이즈: 에너지파 사격 (자이로 조준 + 탭 발사, 위치 이동하는 티라노)
  → 결과 화면 (완전 부활 🦖 / 좀비화 ☠️) → 다시하기
```

---

## 2. 아키텍처 & 기술 스택

```
[데스크탑 브라우저]  ←─ WebSocket ─→  [Node.js 서버]  ←─ WebSocket ─→  [폰 브라우저 ×최대 20]
   (공유 게임 화면)                    (방/상태 관리)                     (컨트롤러)
```

| 파트 | 스택 | 역할 |
|---|---|---|
| `server/` | Node.js + **Socket.IO** + TypeScript | 방 생성/입장, 게임 상태 머신, 뼈 수집 집계, 퍼즐 정답 판정, 에너지파 히트 판정 & 에너지 총량 관리. **서버가 단일 진실 소스** |
| `client/src/desktop/` | React + **PixiJS** + TypeScript | 공유 화면 — 로비 QR, 2분할 뼈 캐기 렌더링, 스켈레톤 조립 퍼즐 보드, 이동하는 티라노/크로스헤어/에너지파 이펙트 |
| `client/src/mobile/` | React + TypeScript | 컨트롤러 — 센서 입력(흔들기·자이로)과 퍼즐 선택 입력만 집계해서 전송, 게임 로직 없음 |
| `shared/` | TypeScript | 이벤트 타입 · 상수 공유 (서버-클라 이벤트 실수 방지) |

**구조 결정 사항**
- 데스크탑/모바일을 별도 앱 2개로 나누지 않고 **클라이언트 앱 하나에 라우트 2개** (`/` = 데스크탑, `/join/:code` = 폰). 개발 서버 1개, QR 주소도 하나라 캠프 일정에서 관리가 쉽다. 코드는 `src/desktop`, `src/mobile` 폴더로 분리 유지
- 모든 상태 변경은 서버에서만 일어나고 `room:state`를 브로드캐스트 → 치팅·동기화 문제 원천 차단
- 센서 이벤트는 폰에서 **로컬 집계 후 전송** (흔들기는 카운트만, 조준 좌표는 30Hz 스로틀). 초당 60회 × 최대 20명 원본 전송은 낭비
- QR에는 서버가 감지한 **LAN IP** (`http://<ip>:5173/join/<code>`)를 넣어 같은 와이파이의 폰이 바로 접속

### ⚠️ 미리 알아야 할 기술적 함정

1. **iOS 센서 권한**: `devicemotion`/`deviceorientation`은 **HTTPS + 사용자 제스처 안에서 `DeviceMotionEvent.requestPermission()` 호출**이 필수. 개발 중에도 ngrok 같은 HTTPS 터널이 필요하다 (2일차에 세팅)
2. **자이로 조준은 절대 좌표가 없다**: "화면 중앙을 겨누고 버튼을 눌러 영점" 잡는 **캘리브레이션** 후 상대 회전량으로 크로스헤어를 움직이는 Wii 리모컨 방식으로 구현
3. **폴백 필수**: 자이로가 이상한 기기 대비, 폰 화면을 터치패드처럼 드래그해서 조준하는 대체 모드를 넣어 시연 리스크를 없앤다
4. **퍼즐 협동 입력 충돌**: 여러 명이 동시에 같은 뼈 조각을 선택할 수 있으므로, 서버가 "먼저 선택한 사람" 기준으로 락을 걸고 나머지는 실패 피드백만 준다

---

## 3. 접속 & 로비

- 데스크탑 `/` 접속 → 서버에 방 생성 요청 → 4자리 코드 + QR(LAN 주소) 표시
- 폰 QR 스캔 → 닉네임(8자 이내) 입력 → 입장. **팀 색상(2종)** + 팀 내 번호로 식별 (인원이 많아 개인별 고유색은 비현실적)
- 서버가 인원 균형 맞춰 A/B팀 자동 배정
- 최소 2명부터 시작 가능, 팀당 최대 10명 (총 최대 20명)
- 연결 끊김 처리: 데스크탑이 끊기면 방 폐쇄, 게임 중 폰이 끊기면 자리 유지(재접속 지원은 후순위)

## 4. 1페이즈 — 뼈 캐기 상세

- **입력**
  - 흔들기: `devicemotion` 가속도 크기가 임계값 초과 시 1회 카운트, **쿨다운 ~200ms**로 중복 방지
  - 폰에서 일정 주기(예: 100ms)마다 "이번 틱에 캔 횟수"만 서버로 전송
- **화면**: 2분할로 팀별 굴착 단면 표시. 흔들 때마다 뼈 조각이 하나씩 발굴되는 연출 + 팀 전체 수집 게이지
- **밸런스 장치(추천)**: 잘못 캐낸 돌덩이(일시적 효율 감소), 특별 화석(보너스 뼈 획득) — 단순 흔들기에 변수를 부여
- **종료 조건**: 팀이 필요한 뼈를 모두 모으면 완료 → 양 팀 모두 완료 시 2페이즈 시작 (한 팀이 먼저 끝나면 대기하며 상대 팀 방해 미니 이벤트 등 후순위 고려)

## 5. 2페이즈 — 뼈 맞추기(퍼즐) 상세

- **입력**: 폰에서 "다음으로 놓을 뼈 조각" 목록 중 하나를 선택 → 데스크탑의 스켈레톤 실루엣에서 정답 부위를 탭해 배치 (드래그 앤 드롭은 정밀도 리스크가 커서 1차 구현에서 제외, 선택형 매칭으로 시작)
- **화면**: 스켈레톤 실루엣 위에 빈 부위가 표시되고, 올바른 조각이 배치될 때마다 뼈가 채워지며 팀 전체가 실시간으로 진행 상황을 확인
- **판정**: 서버가 정답 부위 여부를 검증, 오답 시 페널티 없이 재시도만 허용(협동 게임 흐름 방해 최소화)
- **종료 조건**: 모든 뼈 조각이 올바른 위치에 배치되면 스켈레톤 티라노 완성 → 3페이즈 시작

## 6. 3페이즈 — 되살리기(에너지파 사격) 상세

- **조준**: 폰을 화면 중앙에 겨누고 "조준 시작" 탭 → 영점 캘리브레이션 → 이후 `deviceorientation` 상대값으로 데스크탑에 **팀 색 + 번호 크로스헤어** 이동 (30Hz 스로틀). 팀당 최대 10개 크로스헤어가 동시에 뜰 수 있어 화면 혼잡 방지를 위해 크로스헤어 크기 축소·투명도 조절 등 가독성 처리 필요
- **발사**: **화면 탭**. 서버가 발사 시점 크로스헤어 좌표 vs 스켈레톤 티라노 히트박스 판정
  - (참고) 하드웨어 음량버튼은 웹페이지(미설치 브라우저 탭)에 이벤트를 넘겨주는 표준 API가 없어 iOS/Android 공통으로 사용 불가 → 탭으로 확정
- **티라노 이동**: 스켈레톤 티라노가 일정 주기로 화면 내 위치를 바꿔 조준 난이도를 유지, 약점 부위(눈·심장) 명중 시 에너지 배율 보너스
- **목표**: 팀 에너지 총량이 목표치에 먼저 도달하면 완전 부활 승리
- **실패 연출**: 제한시간 내 목표치 미달 팀은 좀비 티라노 엔딩(배드엔딩) 처리
- **폴백 모드**: 자이로 실패 기기·시연 대비 터치패드 드래그 조준

---

## 7. 프로젝트 구조

```
Tyrannosaurus-rex/
├─ package.json          # npm workspaces + concurrently (npm run dev 하나로 전부 실행)
├─ shared/
│  └─ src/index.ts       # Player, RoomState, GamePhase, 소켓 이벤트 맵, 상수
├─ server/
│  └─ src/index.ts       # Socket.IO 서버 (방 관리 · 상태 머신 · 판정)
└─ client/
   └─ src/
      ├─ desktop/        # DisplayApp — 공유 화면 (로비 → PixiJS 게임 렌더링)
      ├─ mobile/         # ControllerApp — 폰 컨트롤러
      ├─ socket.ts       # 소켓 연결 헬퍼
      └─ main.tsx        # 라우팅: "/" 데스크탑, "/join/:code" 폰
```

**명령어**: `npm install` → `npm run dev` (서버 :3001 + 클라 :5173 동시 실행) · `npm run typecheck`

---

## 8. 일정 (7일)

리스크 큰 순서(접속 → 센서 → 조준)대로 앞에 배치.

| 일차 | 목표 | 상태 |
|---|---|---|
| 1일 | 모노레포 + Socket.IO 서버 + QR 로비: 폰 입장 → 데스크탑 로비 실시간 표시, 팀 자동 배정, 시작 시 페이즈 전환 | ✅ **완료** (스모크 테스트 통과) |
| 2일 | 흔들기 입력 파이프라인 + iOS 권한·HTTPS(ngrok) 처리, **실기기 검증** | |
| 3일 | 1페이즈 완성: PixiJS 2분할 화면, 뼈 수집 동기화, 돌덩이/화석 아이템, 종료 조건 | |
| 4일 | 자이로 조준 프로토타입 + 캘리브레이션 (최대 리스크 — 하루 통째로. 실패 시 터치패드 폴백 확정) | |
| 5일 | 3페이즈 완성: 크로스헤어 다수(팀당 최대 10개) 동시 표시, 히트 판정, 티라노 이동 패턴, 에너지 게이지 | |
| 6일 | 2페이즈(퍼즐) 구현, 결과 화면(부활/좀비 엔딩), 아트/사운드, 페이즈 전환 연출 | |
| 7일 | 실기기 통합 테스트(가용 기기 + 시뮬레이션으로 20명 채움), 밸런스 조정, 버그 픽스, 시연 리허설 | |

### 리스크 관리

| 리스크 | 대비책 |
|---|---|
| iOS 센서 권한/HTTPS | 2일차에 가장 먼저 실기기로 검증, ngrok 상시 사용 |
| 자이로 조준 품질 | 4일차 하루 배정 + 터치패드 폴백 |
| 퍼즐 협동 입력 충돌 | 서버 락 방식으로 동시 선택 처리, UI에서 "누가 잠갔는지" 표시 |
| 와이파이 지연/불안정 | 서버 권위 구조라 순간 끊김에도 상태 복구 용이, 핫스팟 백업 |
| 최대 20명 동시 테스트 인력 | 소켓 시뮬레이션 스크립트로 가짜 플레이어 투입 가능, 실기기는 가용한 만큼만 검증 |

---

## 9. 확장 아이디어 (시간 남으면)

- 게임 중 끊긴 폰의 **재접속** (닉네임+방코드로 자리 복구)
- 1페이즈 방해 아이템 (상대 팀 화면에 흙 튀기기 등 인터랙션)
- 퍼즐 난이도 단계별 뼈 조각 수/오답 함정 조절
- 티라노 패턴 다양화 (에너지파 회피 미니 인터랙션)
- 결과 화면 통계 (분당 캐기 속도, 퍼즐 완성 시간, 명중률)
- 좀비 티라노 엔딩 시 추가 미니게임(정화 미션) 연출
- 관전 모드 / 총원 20명 초과 시 대기열
