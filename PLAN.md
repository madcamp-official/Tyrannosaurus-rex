# 🦖 Tyrannosaurus Rex — 개발 계획

> 몰입캠프 26s-w4-c3-01 · 웹 기반 무가입 크로스플랫폼 파티 게임
> 데스크탑 = 모두가 보는 공유 화면 / 각자의 폰 = 컨트롤러

---

## 1. 게임 개요

- **인원**: 최대 5인 (5인 개인전 또는 2팀전)
- **가입 없음**: 데스크탑에서 방 생성 → 4자리 코드 + QR 표시 → 폰으로 QR 스캔 → 닉네임만 입력하고 입장
- **1페이즈 — 땅 파기**: 티라노사우루스가 묻힌 곳까지 땅을 판다. 폰을 **흔들거나 연타**해서 파며, 데스크탑에는 분할 화면(개인전 5분할 / 팀전 2분할)으로 각자의 진행이 보인다
- **2페이즈 — 티라노 깨우기(사격)**: 개인전은 **과반(3명)**, 팀전은 **한 팀 이상**이 티라노에 도달하면 시작. 폰을 총처럼 들고 조준해 데스크탑 단일 화면 속 티라노를 맞춘다
- **승리**: 개인전 — 누적 데미지 1위 / 팀전 — 팀 합산 데미지 (막타 보너스 옵션)

### 게임 플로우

```
로비 (모드 선택: 5인 개인전 / 2팀전)
  → 1페이즈: 땅 파기 (분할 화면)
  → 전환 연출 ("티라노 발견!")
  → 2페이즈: 티라노 사격 (단일 화면)
  → 결과 화면 → 다시하기
```

---

## 2. 아키텍처 & 기술 스택

```
[데스크탑 브라우저]  ←─ WebSocket ─→  [Node.js 서버]  ←─ WebSocket ─→  [폰 브라우저 ×5]
   (공유 게임 화면)                    (방/상태 관리)                     (컨트롤러)
```

| 파트 | 스택 | 역할 |
|---|---|---|
| `server/` | Node.js + **Socket.IO** + TypeScript | 방 생성/입장, 게임 상태 머신, 파기 깊이 계산, 사격 히트 판정. **서버가 단일 진실 소스** |
| `client/src/desktop/` | React + **PixiJS** + TypeScript | 공유 화면 — 로비 QR, 분할 땅 파기 렌더링, 티라노/크로스헤어/이펙트 |
| `client/src/mobile/` | React + TypeScript | 컨트롤러 — 센서 입력(흔들기·연타·자이로)만 집계해서 전송, 게임 로직 없음 |
| `shared/` | TypeScript | 이벤트 타입 · 상수 공유 (서버-클라 이벤트 실수 방지) |

**구조 결정 사항**
- 데스크탑/모바일을 별도 앱 2개로 나누지 않고 **클라이언트 앱 하나에 라우트 2개** (`/` = 데스크탑, `/join/:code` = 폰). 개발 서버 1개, QR 주소도 하나라 캠프 일정에서 관리가 쉽다. 코드는 `src/desktop`, `src/mobile` 폴더로 분리 유지
- 모든 상태 변경은 서버에서만 일어나고 `room:state`를 브로드캐스트 → 치팅·동기화 문제 원천 차단
- 센서 이벤트는 폰에서 **로컬 집계 후 전송** (흔들기는 카운트만, 조준 좌표는 30Hz 스로틀). 초당 60회 × 5명 원본 전송은 낭비
- QR에는 서버가 감지한 **LAN IP** (`http://<ip>:5173/join/<code>`)를 넣어 같은 와이파이의 폰이 바로 접속

### ⚠️ 미리 알아야 할 기술적 함정

1. **iOS 센서 권한**: `devicemotion`/`deviceorientation`은 **HTTPS + 사용자 제스처 안에서 `DeviceMotionEvent.requestPermission()` 호출**이 필수. 개발 중에도 ngrok 같은 HTTPS 터널이 필요하다 (2일차에 세팅)
2. **자이로 조준은 절대 좌표가 없다**: "화면 중앙을 겨누고 버튼을 눌러 영점" 잡는 **캘리브레이션** 후 상대 회전량으로 크로스헤어를 움직이는 Wii 리모컨 방식으로 구현
3. **폴백 필수**: 자이로가 이상한 기기 대비, 폰 화면을 터치패드처럼 드래그해서 조준하는 대체 모드를 넣어 시연 리스크를 없앤다

---

## 3. 접속 & 로비

- 데스크탑 `/` 접속 → 서버에 방 생성 요청 → 4자리 코드 + QR(LAN 주소) 표시
- 폰 QR 스캔 → 닉네임(8자 이내) 입력 → 입장. 색상 5종 자동 배정
- 모드 토글(개인전/팀전). 팀전 선택 시 서버가 인원 균형 맞춰 A/B팀 자동 배정
- 최소 2명부터 시작 가능, 최대 5명
- 연결 끊김 처리: 데스크탑이 끊기면 방 폐쇄, 게임 중 폰이 끊기면 자리 유지(재접속 지원은 후순위)

## 4. 1페이즈 — 땅 파기 상세

- **입력**
  - 흔들기: `devicemotion` 가속도 크기가 임계값 초과 시 1회 카운트, **쿨다운 ~200ms**로 중복 방지
  - 연타: 화면의 큰 버튼 탭
  - 폰에서 일정 주기(예: 100ms)마다 "이번 틱에 판 횟수"만 서버로 전송
- **화면**: 개인전은 세로 5분할(각자 캐릭터가 파 내려가는 단면), 팀전은 2분할에 팀원 기여 합산. 깊이 게이지 + 실시간 순위
- **밸런스 장치(추천)**: 중간 돌덩이(일시적 효율 감소), 화석 아이템(보너스) — 단순 연타에 변수를 부여
- **종료 조건**: 개인전 — 과반(3명) 도달 / 팀전 — 한 팀 도달 → 2페이즈 시작
- **어드밴티지 연결**: 도달 순위/시간을 2페이즈 보너스(탄약 추가, 먼저 사격 시작)로 이어 1페이즈가 끝까지 의미 있게

## 5. 2페이즈 — 티라노 사격 상세

- **조준**: 폰을 화면 중앙에 겨누고 "조준 시작" 탭 → 영점 캘리브레이션 → 이후 `deviceorientation` 상대값으로 데스크탑에 **플레이어 색 크로스헤어** 이동 (30Hz 스로틀)
- **발사**: **화면 탭**. 서버가 발사 시점 크로스헤어 좌표 vs 티라노 히트박스 판정
  - (참고) 하드웨어 음량버튼은 웹페이지(미설치 브라우저 탭)에 이벤트를 넘겨주는 표준 API가 없어 iOS/Android 공통으로 사용 불가 → 탭으로 확정
- **연출**: 티라노 좌우 이동·포효, 약점 부위(눈·꼬리) 배율 데미지로 조준 실력 반영
- **종료**: 티라노 HP 소진 또는 제한 시간 → 결과 화면
- **폴백 모드**: 자이로 실패 기기·시연 대비 터치패드 드래그 조준

---

## 6. 프로젝트 구조

```
Tyrannosaurus-rex/
├─ package.json          # npm workspaces + concurrently (npm run dev 하나로 전부 실행)
├─ shared/
│  └─ src/index.ts       # Player, RoomState, GamePhase, 소켓 이벤트 맵, 상수
├─ server/
│  └─ src/index.ts       # Socket.IO 서버 (방 관리 · 상태 머신 · 판정)
└─ client/
   └─ src/
      ├─ desktop/        # DisplayApp — 공유 화면 (로비 → PixiJS 게임 렌더링)
      ├─ mobile/         # ControllerApp — 폰 컨트롤러
      ├─ socket.ts       # 소켓 연결 헬퍼
      └─ main.tsx        # 라우팅: "/" 데스크탑, "/join/:code" 폰
```

**명령어**: `npm install` → `npm run dev` (서버 :3001 + 클라 :5173 동시 실행) · `npm run typecheck`

---

## 7. 일정 (7일)

리스크 큰 순서(접속 → 센서 → 조준)대로 앞에 배치.

| 일차 | 목표 | 상태 |
|---|---|---|
| 1일 | 모노레포 + Socket.IO 서버 + QR 로비: 폰 입장 → 데스크탑 로비 실시간 표시, 모드/팀 배정, 시작 시 페이즈 전환 | ✅ **완료** (스모크 테스트 통과) |
| 2일 | 흔들기/연타 입력 파이프라인 + iOS 권한·HTTPS(ngrok) 처리, **실기기 검증** | |
| 3일 | 1페이즈 완성: PixiJS 분할 화면, 깊이 동기화, 돌덩이/아이템, 종료 조건 | |
| 4일 | 자이로 조준 프로토타입 + 캘리브레이션 (최대 리스크 — 하루 통째로. 실패 시 터치패드 폴백 확정) | |
| 5일 | 2페이즈 완성: 크로스헤어 5개 동시 표시, 히트 판정, 티라노 HP/애니메이션 | |
| 6일 | 팀전 마무리, 결과 화면, 아트/사운드, 페이즈 전환 연출, 1→2페이즈 어드밴티지 | |
| 7일 | 실기기 5대 통합 테스트, 밸런스 조정, 버그 픽스, 시연 리허설 | |

### 리스크 관리

| 리스크 | 대비책 |
|---|---|
| iOS 센서 권한/HTTPS | 2일차에 가장 먼저 실기기로 검증, ngrok 상시 사용 |
| 자이로 조준 품질 | 4일차 하루 배정 + 터치패드 폴백 |
| 와이파이 지연/불안정 | 서버 권위 구조라 순간 끊김에도 상태 복구 용이, 핫스팟 백업 |
| 5대 동시 테스트 인력 | 소켓 시뮬레이션 스크립트로 가짜 플레이어 투입 가능 |

---

## 8. 확장 아이디어 (시간 남으면)

- 게임 중 끊긴 폰의 **재접속** (닉네임+방코드로 자리 복구)
- 1페이즈 방해 아이템 (상대 화면 흙 튀기기 등 인터랙션)
- 티라노 패턴 다양화 (돌진 시 회피 미니 인터랙션)
- 결과 화면 통계 (분당 파기 속도, 명중률)
- 관전 모드 / 6인 이상 대기열
